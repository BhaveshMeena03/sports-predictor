fastapi==0.115.0
uvicorn[standard]==0.30.0
anthropic==0.40.0
httpx==0.27.0
aiohttp==3.10.0
python-dotenv==1.0.1
pydantic==2.9.0
sqlite-utils==3.37
aiosqlite==0.20.0
apscheduler==3.10.4
# Dixon-Coles fitting (app/services/poisson.py) — imports fine locally under
# anaconda, then crashes on a clean install without these.
numpy==1.26.4
scipy==1.12.0
# x402: machine payments (USDC on Base). [evm] pulls the eth signing/ABI deps
# the exact-scheme verifier needs — without it the EVM mechanism won't import.
# [extensions] pulls jsonschema, required by the Bazaar discovery
# declaration. Without it the bazaar import fails, install() catches the
# ImportError, and payments silently switch OFF — the paid route starts
# serving free with only a log line to show for it.
x402[evm,fastapi,extensions]==2.16.0
